# Review Manifest

Use `review_fastmcp_manifest` against the provided manifest JSON.

Report:

- High severity findings first.
- Medium and low severity findings after that.
- The exact manifest path for each finding.
- A small fix plan scoped to high severity findings first.
